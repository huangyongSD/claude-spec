---
description: 安全红线规则
globs: ["**/*.java", "**/*.sql", "**/*.vue", "**/*.js", "**/*.yaml", "**/*.yml", "**/*.xml"]
---

# 安全底线规则

> 本文件定义代码安全的不可违反红线。与 governance.md（流程治理）互补：
> - security.md = 安全编码规则（写代码时遵守）
> - governance.md = 安全检查流程（交付时验证）

## 🔴 立即执行（违反即阻断）

### 凭证与密钥

1. **禁止硬编码密钥** — API Key、Token、数据库密码必须通过环境变量注入（application.yml 中使用 `${VAR}` 占位符，不写明文密码）
2. **禁止提交凭证** — .env 中的真实凭证不得提交到 Git（数据库密码、Redis 密码、MQ 认证信息、第三方 API Key 均适用）
3. **禁止在 .claude/ 配置文件中使用真实密码/IP** — 必须使用占位符（如 `{{DB_MASTER_PASSWORD}}`），真实值存储在 `.claude/tools/secrets.json`（本地，不提交 Git）。AI 生成代码时也必须使用占位符，不得复制上下文中看到的真实敏感值

### SQL 安全

4. **禁止 SQL 拼接** — 必须使用参数化查询（MyBatis 中使用 `#{}` 而非 `${}`，`${}` 存在注入风险）
5. **SQL 脚本必须标注数据库类型** — `-- DB_TYPE: PostgreSQL`（见 CLAUDE.md 数据库规范和 dbinfo.md）

### Web 安全

6. **禁止 XSS 风险** — 用户输入必须做转义处理，前端 `v-text` 优先于 `v-html`
7. **禁止明文传输敏感数据** — HTTPS 强制，非必要不走 HTTP

### 后端编码安全

8. **API 响应层 nil 集合兜底** — 序列化前拦截 nil 集合类型，返回空数组/列表 `[]` 而非 `null`
9. **写操作校验直属关系** — 防止 IDOR 横向越权，必须校验操作者与目标的直属关系
10. **用 VO/DTO 接收前端参数** — 禁止直接绑定数据库 DO/Entity，防止字段注入
11. **权限注解用 hasPermi 非 hasRole** — `@PreAuthorize("@ss.hasPermi('xxx:action')")` 而非 `@PreAuthorize("hasRole('MANAGER')")`。角色检查 ≠ 权限检查

### 前端安全

> 前端防御性消费和错误处理见 frontend.md 🔴1-5

## 🟡 新代码执行

### 权限一致性

1. **前后端权限同步维护** — 前端路由守卫和后端 API 权限必须同步
   - 前端允许访问但后端拒绝 = 用户看到 403 报错
   - 后端允许访问但前端隐藏 = 安全漏洞
2. **菜单权限与 API 权限同步** — 新增菜单必须配置 `perms` 字段，且与后端 `@PreAuthorize` 一致
3. **按钮权限显隐** — 操作按钮使用 `v-hasPermi` 指令，无权限时隐藏

### 依赖与变更安全

4. 新增依赖需说明用途，并确认非恶意包
5. 数据库迁移前必须备份，并保留回滚方案（见 design.md §2.3）

## 执行方式

🔴 级别违反，代码审查时阻断，涉及凭证/密钥的违反要求用户确认后立即修复。
🟡 级别违反，代码审查时检查。

## 关键洞察

- **nil 集合序列化为 null 后前端 `.map()` 直接崩溃**：响应层统一兜底是最高效的修复点（详见 frontend.md 🔴1-4）
- **角色检查 ≠ 权限检查**：`hasRole('MANAGER')` 只验证角色，`@ss.hasPermi('xxx:action')` 验证功能权限。写操作必须用权限检查
- **字段注入是隐形漏洞**：直接绑定 DO 意味着攻击者可以传入数据库有但表单没有的字段（如 `id`、`deleted`、`tenantId`）

## 变更记录

| 日期 | 变更内容 | 原因 |
|------|----------|------|
| 2026-04-28 | 删除 🔴12（Controller不写业务逻辑），已在 CLAUDE.md 覆盖 | 职责越界：架构规则非安全规则 |
| 2026-04-28 | 删除 🔴13/🔴14（前端防御性消费/静默失败），改为交叉引用 frontend.md | 职责越界：前端编码规则非安全规则，且与 frontend.md 重复 |
| 2026-04-28 | 删除 🟡5（权限代码双重检查），移至 governance.md | 职责越界：审查流程属流程治理 |
| 2026-04-28 | 删除 🟡6（迁移备份），与 governance.md P2-14 重复 | 重复规则 |
| 2026-04-28 | 执行方式从"Hook直接阻断"改为"代码审查时阻断，凭证/密钥违反要求用户确认" | 与实际 Hook 能力不符 |
| 2026-04-28 | 关键洞察去重，nil 集合洞察改为交叉引用 frontend.md | 消除与规则条文的重复 |
