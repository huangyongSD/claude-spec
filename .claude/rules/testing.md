---
description: 测试底线规则
globs: ["**/*Test.java", "**/*Tests.java", "**/*test*", "**/*.spec.js", "**/*.test.js", "playwright.config.*", ".claude/specs/**/test-cases.md"]
---

# 测试底线规则

> 本文件定义测试的不可违反红线和覆盖要求。与 governance.md（流程治理）互补：
> - testing.md = 测试编码规则（写测试时遵守）
> - governance.md = 测试检查流程（交付时验证）

## 🔴 立即执行（违反即阻断）

### AC 类型最低覆盖要求

> 每个 AC 必须有测试覆盖，覆盖标准按 AC 类型区分。

| AC 类型 | 最低覆盖要求 | 说明 |
|---------|------------|------|
| 功能类 | 1 UT + 1 E2E（正向+反向） | 单元测试验证逻辑，E2E 验证端到端 |
| 权限类 | 1 E2E（有权）+ 1 E2E（无权）+ 1 IDOR | 权限测试是安全底线 |
| 数据类 | 1 UT（校验逻辑）+ 1 E2E（异常场景） | 数据校验适合单元测试 |
| 交互类 | 1 E2E（交互验证） | 筛选/排序/分页等交互 |
| 性能类 | 人工验证（标注原因） | 当前无自动化性能测试 |

### 测试禁止模式

| 禁止模式 | 示例 | 正确做法 |
|----------|------|---------|
| 吞掉失败 | `.catch(() => null)` | 让错误直接抛出 |
| 条件断言 | `if (resp) expect(...)` | 无条件断言 |
| 只验证 DOM 存在 | 仅 `toBeVisible()` / `toBeInTheDocument()` 就结束 | 验证数据内容非空 |
| 硬等待 | `waitForTimeout(3000)` | 等待具体条件 |
| 跳过测试 | `it.skip` / `@Disabled` | 修复测试，不跳过 |
| 只测 happy path | 只用有数据的管理员 | 必须覆盖空状态和无权限 |

### E2E 测试基础设施（强制执行）

> **前置条件**：项目必须在初始化时通过 `/sds-init` 配置 Playwright 框架（见 sds-init SKILL.md）。E2E 测试是质量门禁的必选项，不允许降级。

1. **全局错误监听** — 必须同时监听 `pageerror` 和 `console error`，断言前等待 `domcontentloaded` + 500ms
2. **三层验证标准** — 每个测试必须验证（缺一不可）：
   - 页面可达：URL 正确，主容器可见
   - 数据加载：至少一个 API 请求返回成功
   - 数据渲染：至少一个数据驱动的 DOM 元素包含非空文本（非占位符）

## 🟡 新代码执行

### 测试覆盖

1. 新增函数必须附带单元测试
2. 代码提交必须通过全部 CI 测试
3. 测试覆盖率下降不允许合并
4. 调试打印代码在提交前必须清除（前端 `console.log`，后端 `System.out.println`）
5. 禁止跳过测试的标注提交到主分支
6. 构建前必须通过本地测试（`mvn test`）

### 测试数据规范

7. **测试账号覆盖 4 种角色** — 超级管理员、普通用户、无权限用户、无数据用户。不要只测有数据的用户
8. **必须包含"无数据"用户** — 测试空状态和 nil 兜底
9. **测试数据通过 seed 脚本准备** — 可重复执行，幂等
10. **不要假设数据库状态** — 直接查询验证，数据库里可能只有 M 种角色，代码定义了 N 种

### 需求驱动测试

11. 从 requirements.md 提取 AC 清单
12. 为每个 AC 编写对应测试用例，标记完成状态
13. 补写测试直到每个 AC 均有对应测试覆盖
14. 每个 BR 必须有对应 UT（业务规则是核心逻辑）

## 🟢 逐步达标

### 覆盖率目标

1. 核心业务逻辑测试覆盖率逐步达到 80%
2. 所有 API 端点有对应测试用例

### 分层测试策略

```
┌─────────────┐
│  batch-d    │  流程测试（serial，端到端完整流程）
├─────────────┤
│  batch-e    │  状态流转测试（正向 + 非法转换）
├─────────────┤
│  batch-c    │  权限测试 + IDOR 横向越权
├─────────────┤
│  batch-b    │  功能测试（正向 + 反向 + 边界值）
├─────────────┤
│  batch-a    │  冒烟测试（页面可达 + 数据加载 + 数据渲染）
└─────────────┘
```

各层职责不可互相替代。冒烟测试保证页面能打开，功能测试保证逻辑正确，流程测试保证端到端可用。

## 测试命令

- 单元测试：`mvn test`（后端）/ 无（前端，暂无测试脚本）
- E2E 测试：`npx playwright test`（当前未配置）
- 覆盖率：无

## 执行方式

🔴 级别违反，测试不通过，阻断合并。
🟡 级别在代码审查时检查，新代码不合规不合并。
🟢 级别记入技术债清单，排期整改，不阻塞当前开发。

## 关键洞察

- **测试全绿 ≠ 没有 bug**：只测有数据的用户等于只测了 happy path。测试账号的选择偏差会制造虚假的安全感
- **测试基础设施本身也会坏**：登录 helper 字段名错误会导致所有测试静默失败，测试看似在跑实则形同虚设
- **覆盖率审计必须独立**：编写测试的 SDA 不能自己审计覆盖率，避免自我评价偏差
- **权限测试不可省略**：每个权限类 AC 必须有 IDOR 测试，否则横向越权漏洞无法被发现

## 变更记录

| 日期 | 变更内容 | 原因 |
|------|----------|------|
| 2026-04-28 | "只验证DOM存在"示例补充 `toBeInTheDocument()` | 表述更准确 |
| 2026-04-28 | 🟢1 删除"当前：0%"硬编码状态 | 规则文件不应包含项目状态快照 |
